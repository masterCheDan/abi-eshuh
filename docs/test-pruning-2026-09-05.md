# 测试精简检查

日期：2026-09-05。范围仅为测试、测试辅助代码及一处过时的检查脚本注释；未修改应用或引擎业务逻辑，未提交。

## 结论

252 项并非每项都有独立保护价值。主要冗余来自不同阶段重复添加同一回归、跨层重复枚举牌序，以及效果不变的技能等级重复跑完整引擎。

精简后：**28 个测试文件、220 项通过，净减少 32 项**。删除了 38 个旧执行用例，将必要的字段拒绝、同帧顺序和未登记自动触发检查重新安置为 6 个有明确归属的用例。没有通过跳过测试或限制测试文件匹配来减少数量。

## 删除依据与保留位置

| 调整 | 依据及保留的保护 |
| --- | --- |
| 删除 `baseline/knownIssues.test.ts` 的 6 项 | B01 归入 catalogCoverage；B02 归入 triggerFacts；B03 归入 v4；B04 归入生产 planTransfer。全套测试不再额外启动 CLI，再由 CLI 重启 Vitest；独立 CLI 检查和 CI 门禁仍保留。 |
| 删除 `baseline/shareReplay.test.ts` 的 1 项及无调用的 roundTripScenario | 默认等级回放与生产分享测试重复；非默认等级回放统一检查完整编队、环境、召唤引用、人工事实、输入不变性及 SimulationResult。 |
| 删除引擎中的 10 项重复检查 | 牌序枚举留在 cardOrderSystem；引擎保留真实手牌/抽牌堆集成、无自定义牌序的合法/非法边界及三人队退化情况。失败不扣费、不消费减费次数、不推进牌序由更严格的完整结果对照回归保护。 |
| 乐队等级执行从 20 项减至 9 项 | 常量层数只跑最低/最高等级；和纱保留 Lv1、Lv4、Lv5，覆盖数值跳变两侧。保留独立的实际层数数据断言、封顶、衰减、累计授予及倍率消费测试。 |
| 删除 2 项门面/登记快照检查 | 重复查询固定登记值；真实消费路径已有卡牌、目标、欠费、召唤和变身行为测试。学生定义转换及技能视图测试保留。 |
| 删除 1 项 NS 子集检查 | 同文件完整字典相等断言已经包含“不多登记学生”的要求。 |
| 删除 2 项普攻片段弱断言 | 同一输入已有完整片段序列断言；重复验证间隙和类型集合没有额外价值。不同打断时机的行为测试保留。 |
| 精简旧版迁移与非法导入用例 | v0.0.1/v1 的空事件迁移重复执行被删除，默认值/提示断言放入已有带事件迁移。部分非法字段原先解码为 null 后才交给导入器，实际只重复测 null 拒绝；现在直接在 codec 层断言字段拒绝。 |

分享主回归同时修正了一处测试设计问题：原测试在同一帧插入两次技能，可能只证明“两边同样失败”。现在主回归要求原方案无错误、召唤目标上的 NS 数值确为 10，之后再比较完整重放结果；事件 ID 不得改变同帧顺序另由轻量 codec 测试负责。

## 未删除的相似测试

- 未知 Effect、Target、Stat、条件参数与操作符是不同校验分支，不按名称相似删除。
- CostChange 的数值计算、引擎扣费及审计回读分别保护不同消费者，不能只留其中一层。
- 受控 EX 与自动 NS、召唤过期与替换、固定减费与比例减费有不同输入或生命周期，保留独立行为检查。
- 旧分享版本迁移、原子失败、规则缺失负例和学生特有机制仍保留。

## 验证与历史

- 精简前：252 项通过；精简后：220 项通过，0 失败。
- 相同本机命令的单次墙钟测量：9.39 秒 → 5.35 秒。包含进程启动，不是严格性能基准；主要移除了全套测试内的重复测试进程启动。
- 类型检查、ESLint、独立规则登记检查通过；规则分级位置数保持 2251 / 907 / 1285 / 2516，errors 为空。
- 第一、二、三步报告保持原样，其测试数量是各阶段的历史记录。
- 删除的两份基线测试文件原为未跟踪文件，不能直接用 `git restore` 恢复；原内容已在本次会话检查输出中保留，可据此还原。已有其他未提交修改保持不动。
